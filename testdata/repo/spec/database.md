---
title: Database
includes: [RFC-0001, RFC-0004, ADR-0001]
---

# Database

The database is PostgreSQL. Connections are pooled per process.

## Configuration

The connection string is read from the environment.

## Configuration

A repeated heading, so its anchor is suffixed as GitHub suffixes it.
